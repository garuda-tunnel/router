apiVersion: v2
name: ipt-server
description: "Garuda ipt_server (garuda_ipt + powerdns) pod with optional FRR sidecar."
type: application
version: 1.0.0
appVersion: "0.1.0"

dependencies:
  - name: frr-sidecar
    version: "0.2.0"
    repository: "oci://ghcr.io/garuda-tunnel/charts"
